# Core ML — loose pins so it works with whatever Python you've got
tensorflow>=2.15.0
ultralytics>=8.3.40
torch>=2.0.0
torchvision>=0.15.0

# Inference utils
numpy>=1.24.0          # we use the np.trapz shim if 2.x is installed
opencv-python>=4.8.0
pillow>=10.0.0
pandas>=2.0.0
scikit-learn>=1.3.0

# UI
streamlit>=1.30.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Dev / testing
pytest>=7.4.0
pytest-cov>=4.1.0
